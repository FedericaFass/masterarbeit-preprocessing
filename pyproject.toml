[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ppm-preprocessing"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "pandas",
  "numpy",
  "pm4py",
  "pyarrow"
]

[tool.setuptools.packages.find]
where = ["src"]
